<!DOCTYPE html>
<html>
  <head>
    <title>Admin - Recosnapo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/a05b89f759.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  
  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light">
       <div class="container-fluid">
         <a class="navbar-rogo" href="/"><%= image_tag 'recosanpo.png' %></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if admin_signed_in? %>
              <li class="nav-item">
                <%= link_to '管理者ログアウト', destroy_admin_session_path, method: :delete, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
               <%= link_to admin_users_path,class: 'nav-link text-dark' do %>
                <%= "全ユーザー一覧" %>
                <% end %>
              </li>
              <li><%= render 'public/posts/search' %></li>
              <% end %>
            </ul>
         </div>  
        </div>
      </nav>
      <div class="container-fluid">
        <div class="row px-3 px-4">
          <div class="row">
           <%= image_tag 'sora01.jpg', class: 'img-fluid' %>
          </div>
        </div>
      </div>
    </head>
    
    <%= flash[:notice] %>
    <%= flash[:alert] %>

    <%= yield %>
    <footer class="bg-light mt-5 py-3">
      <div class="container">
        <div class="row no-gutters">
          <div class="container-fluid text-center">
            <p>©RECOSANPO Coeporation 2024</p>
          </div>
       </div>
      </div>
    </footer>
  </body>
</html>

